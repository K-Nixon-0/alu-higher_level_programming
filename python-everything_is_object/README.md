# Python - Everything is Object

Quiz project on Python's object model: `type()`/`id()`, mutable vs immutable, `==` vs `is`, string/int interning, shallow copies.

## Tasks
0-18, 20-28: `N-answer.txt` short-answer quiz files.
19: `19-copy_list.py` — returns a copy of a list.

## Author
Guillaume
