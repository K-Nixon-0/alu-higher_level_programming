# SQL - More Queries

Users/privileges, constraints (NOT NULL, DEFAULT, UNIQUE, PRIMARY KEY, FOREIGN KEY), and JOINs (INNER/LEFT), subqueries, GROUP BY.

## Tasks
0-16: individual `.sql` scripts.

## Author
Guillaume
