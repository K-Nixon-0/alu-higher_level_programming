# Python - Almost a Circle

OOP project building a `Base` → `Rectangle` → `Square` hierarchy: private attributes with validated properties, `*args`/`**kwargs` updates, JSON serialization, and file persistence.

## Structure
