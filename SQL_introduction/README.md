# SQL - Introduction

Basic SQL: databases, tables, DESCRIBE, INSERT, SELECT, WHERE, ORDER BY, GROUP BY, AVG.

## Tasks
0-15: individual `.sql` scripts, run via `cat file.sql | mysql -uroot -p`.

## Author
Guillaume
