# Python - Object-relational mapping

Connects Python to MySQL: raw `MySQLdb` queries (with SQL-injection-safe parameterized queries), then SQLAlchemy ORM models, sessions, filtering, insert/update/delete, and joins.

## Author
Guillaume
